{
    "name": "Default Linux Universal",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "workspaceFolder": "/workspaces/cis476-term-project",
    "forwardPorts": [3000],
    "postCreateCommand": "cd term-project-frontend && npm install",
    // "remoteUser": "root"
}